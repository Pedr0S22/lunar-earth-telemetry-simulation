# lunar-earth-telemetry-simulation
Engineered a containerized architecture with docker that simulates the telemetry pipeline of a Lunar Surface Rover communicating with a Gateway Orbiter. The main goal was to create a ingestion pipeline that decouples sensors data from data persistence via a message broker. Kafka and TICK stack was used to ensure near real time data reception.
